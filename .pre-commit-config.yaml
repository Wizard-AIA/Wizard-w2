repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.6.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-added-large-files
      - id: check-merge-conflict

  # Kept in step with the pinned version in .github/workflows/ci.yml, so a
  # commit that passes the hook also passes the lint gate.
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.14.14
    hooks:
      # Run the linter
      - id: ruff
        args: [ --fix ]
      # Run the formatter
      - id: ruff-format

  - repo: https://github.com/alessandrojcm/commitlint-pre-commit-hook
    rev: v9.16.0
    hooks:
      - id: commitlint
        stages: [commit-msg]
        additional_dependencies: ['@commitlint/config-conventional']
        # Use the system Node rather than nodeenv downloading its own: nodeenv's
        # "latest stable" lookup fetches https://nodejs.org/dist/index.json via
        # Python's http.client, which truncates the response on some platform/
        # OpenSSL combinations (reproducible outside pre-commit too; curl is
        # unaffected). Avoids the fetch entirely instead of chasing that bug.
        language_version: system
